{
  "name": "@arbiter/langchain",
  "version": "0.1.0",
  "description": "LangChain tools for Arbiter — pre-flight judgment for agents that hold a wallet.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "peerDependencies": {
    "@langchain/core": ">=0.3.0 <2.0.0"
  },
  "dependencies": {
    "@arbiter/sdk": "^0.1.0",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@langchain/core": "^1.2.4",
    "@types/node": "^24.7.0",
    "typescript": "^5.9.3"
  }
}
